/*
Quick Quiz: Use the library function to 
calculate the area of a square with side a.
*/
#include <stdio.h>
#include <math.h>
int main() {
    int a=5;
    printf("The area of the sq is %f\n",pow(a,2));
    return 0;
}